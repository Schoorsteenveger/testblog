<%= form_for(post) do |f| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="container" id="bg-form">
    <div class="field">
      <h2><%= f.label :title %></h2>
      <p><%= f.text_field :title %></p>
    </div>

    <div class="field">
      <h3><%= f.label :body %></h3>
      <p><%= f.text_area :body %></p>
    </div>

    <div class="actions">
      <h3><%= f.submit :Save %></h3>
    </div>
  </div>
  
<% end %>
